# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com
version: 2
before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
    # Build the embedded sshpiperd-webadmin frontend before any go build.
    # Requires Node.js 18+ and npm on the host.
    - npm --prefix cmd/sshpiperd-webadmin/internal/httpapi/web ci
    - npm --prefix cmd/sshpiperd-webadmin/internal/httpapi/web run build
    # Build the linux binaries inside the Dockerfile's `bin-export` stage and
    # extract them to dist/docker-bins/. Each linux build below has a `post`
    # hook that overwrites the goreleaser-built binary with the matching
    # extracted one, so the bytes shipped in the runtime image and in the GH
    # release archives are identical (no `EXTERNAL=1` shim needed).
    - make docker-bins
builds:
  - id: sshpiperd
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./cmd/sshpiperd
    binary: sshpiperd
    hooks:
      post:
        - &replace_with_docker_bin 'scripts/goreleaser-replace-with-docker-bin.sh "{{ .Path }}" {{ .Os }} {{ .Arch }} "{{ .Name }}"'
    flags:
      - &trimpath_flag -trimpath
    ldflags:
      - &strip_ldflags -s -w
      - -X main.mainver={{.Version}}
  - id: snaplauncher
    env:
      - CGO_ENABLED=0
    goos:
      - linux      
    main: ./cmd/sshpiperd/snap/launcher
    binary: snap/launcher
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    hooks:
      pre: go generate ./cmd/sshpiperd/snap/launcher
    goarch:
      - amd64
      - arm64      
  - id: plugin_workingdir
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/workingdir
    binary: plugins/workingdir
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_yaml
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/yaml
    binary: plugins/yaml
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_docker
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      # - windows
      # - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/docker
    binary: plugins/docker
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_kubernetes
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      # - windows
      # - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/kubernetes
    binary: plugins/kubernetes
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_fixed
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/fixed
    binary: plugins/fixed
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_failtoban
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/failtoban
    binary: plugins/failtoban
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_username_router
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/username-router
    binary: plugins/username-router
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_lua
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/lua
    binary: plugins/lua
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: plugin_metrics
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./plugin/metrics
    binary: plugins/metrics
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
    tags:
      - full
  - id: sshpiperd_webadmin
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./cmd/sshpiperd-webadmin
    binary: sshpiperd-webadmin
    hooks:
      post:
        - *replace_with_docker_bin
    flags:
      - *trimpath_flag
    ldflags:
      - *strip_ldflags
      - -X main.mainver={{.Version}}
    tags:
      - full

archives:
  - formats: ['tar.gz']
    allow_different_binary_count: true
    # this name template makes the OS and Arch compatible with the results of uname.
    name_template: >-
      sshpiperd_with_plugins_
      {{- .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: ['zip']
    ids:
      - sshpiperd
      - plugin_workingdir
      - plugin_yaml
      - plugin_fixed
      - plugin_failtoban
      - plugin_docker
      - plugin_kubernetes
      - plugin_username_router
      - plugin_lua
      - plugin_metrics
      - sshpiperd_webadmin
# Docker images are not built by GoReleaser. The Dockerfile is the single
# source of truth for the linux binaries: `make docker-bins` runs in the
# `before:` hooks above to extract them from the `bin-export` stage, and
# every linux build's `hooks.post` swaps the goreleaser-compiled binary for
# its docker-extracted twin (see `scripts/goreleaser-replace-with-docker-bin.sh`).
# The published multi-arch images are pushed by `make docker-push` (called
# from `.github/workflows/release.yaml` after this goreleaser run), reusing
# the same `Dockerfile` and the buildx cache populated by `docker-bins`.
checksum:
  name_template: "checksums.txt"
snapshot:
  version_template: "{{ incpatch .Version }}"
release:
  draft: true
  replace_existing_draft: true
  disable: true

snapcrafts:
  - ids:
      - sshpiperd
      - snaplauncher
      - plugin_workingdir
      - plugin_yaml
      - plugin_fixed
      - plugin_failtoban
      - plugin_username_router
      - plugin_lua
    name: sshpiperd
    name_template: "sshpiperd_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    summary: The missing reverse proxy for ssh scp
    description: |
      sshpiperd is a reverse proxy for ssh/scp. It allows you to have a single
      ssh key pair for all your servers, behind a single IP address. It also
      allows you to connect to internal servers without exposing them to the
      internet.
    publish: true
    grade: stable
    confinement: strict
    license: MIT
    apps:
      sshpiperd:
        command: launcher
        plugs: [network-bind] 
        daemon: simple
    channel_templates:
      - beta
      - stable
    extra_files:
      - source: cmd/sshpiperd/snap/hooks/configure
        destination: meta/hooks/configure
        mode: 0755        
    hooks:
      configure:
        plugs: [home]
